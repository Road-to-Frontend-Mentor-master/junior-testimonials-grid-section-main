include ../blocks/user
include ../blocks/card
include ../data/cards

doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Frontend mentor
  body
    main
      each cardInfo in cardsInfo
        +card(cardInfo.title, cardInfo.comment)
          +user(cardInfo.header.pictureUrl, cardInfo.header.authorName, cardInfo.header.profession)
